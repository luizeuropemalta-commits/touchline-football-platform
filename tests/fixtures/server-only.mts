// Node's contract tests execute server modules outside Next.js. The production
// `server-only` boundary is exercised by Next; this inert fixture only lets
// pure server-side planning functions be tested under node:test.
export {};
