import { ComingSoon } from "@/components/coming-soon";

export default function PlayerApiSearchPage() {
  return (
    <ComingSoon
      eyebrow="Touchline / Legacy API workspace"
      title="Old player API search archived"
      description="This older external player search is no longer the main workflow. Use Football Search and the Football Data Center so Touchline reads from the provider layer and internal database."
    />
  );
}
